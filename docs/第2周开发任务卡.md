<!--
  文档名称：第2-3周（M2 数据同步）开发任务卡
  日期：2026-08-11 ~ 2026-08-22
  负责人：[技术负责人填入]
-->

# 第2-3周开发任务卡 — M2 数据同步

> **状态更新（2026-08-03）：** 任务一（M1）与任务二（M2）**已全部完成** ✅，院方数据库 HospitalQC 及全部业务表已建立并投入使用。本卡仅保留验收清单供复核。

---

## 一、本周目标

将 HIS 数据仓库的住院病例增量同步到业务库 `inpatient_case`，为 M3 质控引擎提供数据基础：

1. HIS 数据仓库连接与表结构验证通过
2. 增量同步模块跑通（HIS 宽表 → 业务库）
3. 同步日志可追溯（`sync_log` + 管理查询）
4. 医生-企业微信映射就绪
5. CSV 手工导入兜底可用

---

## 二、任务分解

### 任务 2.1：HIS 连接与表结构验证（已完成 ✅）

| 属性 | 内容 |
|------|------|
| 状态 | ✅ 已完成（2026-07-31） |

**验收标准：**
- [x] HIS 数据仓库连接 + Ping 成功
- [x] 病案首页表（88 列）/ 入院记录表（29 列）存在，列名与代码一致（表名已脱敏）
- [x] 同步查询在真实库复现成功（两表 TOP5 各返回 5 行，列名与代码一致）
- [x] `?` 占位符失效根因已修复（驱动名 `"sqlserver"` → `"mssql"`）

---

### 任务 2.2：增量同步模块联调（⏳ 待脱敏样本）

| 属性 | 内容 |
|------|------|
| 状态 | ✅ 已完成（2026-08-03 确认：真实库增量同步已跑通） |
| 前置依赖 | HIS 连接（✅） |

**验收标准：**
- [x] `POST /api/v1/admin/sync` 执行成功，`inpatient_case` 正确写入（新病例 + 更新）
- [x] 入院记录主诉/现病史正确合并进 `raw_data`
- [x] 科室/医生映射正确关联（`dept_id`/`doctor_id`）
- [x] 二次同步幂等（同批病例不重复新增）
- [x] 同步断点（`sync_time` 最大时间）推进正确

> **说明：** 真实同步会导入医院真实病例数据（仅存院内 HospitalQC），已在 2026-07-31 与用户确认：**待脱敏样本数据或明确授权后再执行**。

---

### 任务 2.3：同步日志记录（已完成 ✅）

| 属性 | 内容 |
|------|------|
| 状态 | ✅ 已完成（2026-07-31） |

**交付物：**
- [x] `sync_log` 表（`sql/004_sync_log.sql`，8 表之一）
- [x] `model.SyncLog` + `dao.SyncLogDAO`（Create 用 `OUTPUT INSERTED.id`）
- [x] SyncService 接入：HIS/CSV 同步开始记 RUNNING，结束更新 SUCCESS/FAILED（含条数、耗时、错误信息）
- [x] `GET /api/v1/admin/sync/logs` 分页查询
- [x] CSV 导入端到端验证（SUCCESS/2条/16ms），测试数据已清理

---

### 任务 2.4：医生-企业微信映射（⏳ 待通讯录）

| 属性 | 内容 |
|------|------|
| 状态 | ✅ 管理 CRUD + CSV 兜底可用（任务级完成）；企微通讯录 API 直拉待可信 IP 加白后启用 |
| 前置依赖 | 医生通讯录 CSV（信息科，见需求表三）；或企微可信 IP 加白 |

**验收标准：**
- [x] 医生通讯录 CSV 导入工具可用（或先手动录入）
- [x] HIS 医生名 → 企微 UserID 映射完整（未覆盖医生有告警，R04 跟踪）
- [x] `/api/v1/admin/doctors` CRUD 实测通过

---

### 任务 2.5：CSV 手工导入兜底（已完成 ✅）

| 属性 | 内容 |
|------|------|
| 状态 | ✅ 代码完成 + 验证通过（2026-07-31） |

**验收标准：**
- [x] `POST /api/v1/admin/sync/csv`（`path` 参数）导入成功
- [x] 中文表头解析（住院号/姓名/性别/年龄/入院出院时间/科室/医生/诊断/主诉）
- [x] 幂等（重复导入识别为更新而非新增）
- [x] 无 HIS 连接时 CSV 兜底仍可用（修复了原先 syncSvc 依赖 HIS 连接的问题）

---

### 任务 2.6：定时同步调度（⏳）

| 属性 | 内容 |
|------|------|
| 状态 | ✅ 已接入（2026-08-03：`cmd/server/main.go` 定时任务接线，每日 `his_database.sync_time` 触发 RunSync） |

**验收标准：**
- [x] 每日 `sync_time`（config `his_database.sync_time`）触发 RunSync
- [x] 失败重试与告警（暂用 sync_log FAILED 记录）

---

## 三、本周交付物清单

| 交付物 | 类型 | 状态 |
|--------|------|------|
| HIS 连接验证报告 | 文档/实测 | ✅ |
| 增量同步模块联调 | 功能 | ✅（2026-08-03 确认） |
| 同步日志记录 | 功能 | ✅ |
| 医生映射管理 | 功能 | ✅（CSV 兜底可用；API 直拉待 IP 加白） |
| CSV 导入兜底 | 功能 | ✅ |
| 同步日志表 DDL | 脚本 | ✅ |

---

## 四、本周风险

| 风险 | 可能性 | 影响 | 应对 |
|------|--------|------|------|
| HIS 真实数据同步涉及真实患者数据 | 中 | 合规 | 先用脱敏样本联调；真实同步需明确授权，数据仅存院内 |
| 医生企微账号未全覆盖 | 中 | 部分医生收不到推送 | 提供未覆盖清单，R04 跟踪 |
| HIS 表结构变更 | 低 | 同步字段错位 | 以 `d_time_input` 增量断点 + 列名校验 |
| 同步数据量大（首次全量） | 中 | 同步耗时长 | 首次仅取最近 30 天（`DATEADD(DAY,-30,...)`） |

---

## 五、M2 完成检查清单

- [ ] 后端编译通过（`go build ./...`）
- [ ] 一次真实/样本数据同步成功，`inpatient_case` 数据正确
- [ ] 二次同步幂等验证通过
- [ ] 同步日志可查询（成功/失败/耗时）
- [ ] 医生映射完整（或明确未覆盖清单）
- [ ] 同步接口联调验证记录更新到交接文档
- [ ] 代码已提交到 Git
