<!--
  文档名称：第3-4周（M3 质控引擎 + M4 消息推送）开发任务卡
  日期：2026-08-18 ~ 2026-08-29
  负责人：[技术负责人填入]
  状态：M3 引擎修复/接线已完成（2026-08-03），本卡列出剩余工作与验收标准
-->

# 第3-4周开发任务卡 — M3 质控引擎 + M4 消息推送

> **前置状态（2026-08-03）：**
> - M1 基础设施 ✅ 已完成
> - M2 数据同步 ✅ 已完成（院方数据库 HospitalQC 及业务表已建立并投入使用）
> - M3 引擎代码 ✅ 已修复并通过全部运算符单元测试（`go test ./...`）
> - M4 推送服务 ✅ 已接线（PushService/Pusher/令牌桶/免打扰/推送日志/定时任务）

---

## 一、本周目标

1. 质控引擎在真实病例数据上跑通（`POST /admin/qc/run` 返回批次结果）
2. 推送链路打通（质控 → 缺陷聚合 → 模板卡片 → push_log 追踪）
3. 规则 CRUD + 首批业务规则清单确认
4. 前端（M5）依赖的后端接口全部就绪（报告详情/确认整改/待整改列表/科室统计）

---

## 二、任务分解

### 任务 3.1：规则清单确认与入库（⏳ 待 PM/质控科）

| 属性 | 内容 |
|------|------|
| 状态 | ⏳ 依赖首批业务质控规则清单（PM 目标第2周末，尚未到位） |
| 说明 | 当前使用 `sql/002_init_data.sql` 的 10 条示例规则联调 |

**验收标准：**
- [ ] 质控科确认首批规则清单（规则编码/名称/等级/表达式/阈值）
- [ ] 规则通过 `/api/v1/admin/rules` 写入 `qc_rule` 表（或由 sql 脚本导入）
- [ ] 每条规则在脱敏样本上验证命中/不命中符合预期

---

### 任务 3.2：真实病例质控联调（✅ 代码就绪，待数据/规则）

| 属性 | 内容 |
|------|------|
| 状态 | ⏳ 引擎修复已完成（2026-08-03），待真实数据跑批验证 |

**验收标准：**
- [ ] `POST /api/v1/admin/qc/run` 在 HospitalQC 真实数据上执行成功
- [ ] `qc_result` 正确写入（is_defect 标记 + 缺陷详情/建议）
- [ ] `inpatient_case.qc_status` 正确更新（PENDING → PASSED/ISSUED）
- [ ] 质控完成后自动触发推送（ISSUED 病例 → M4）

---

### 任务 3.3：推送真机联调（⏳ 待企微可信 IP 加白）

| 属性 | 内容 |
|------|------|
| 状态 | ⏳ 代码已就绪（PushService + 定时任务接线），被「企微可信 IP 白名单」阻塞 |
| 前置依赖 | 企微管理后台将部署服务器公网 IP 加入「企业可信 IP」+ 确认通讯录读取权限 |

**验收标准：**
- [ ] 企微后台加白后，在服务器运行 `go run ./cmd/weworkcontact` 拉取通讯录成员
- [ ] 医生-企微映射写入 `doctor_wework`（API 直拉优先，CSV 兜底）
- [ ] `POST /api/v1/admin/qc/run` 触发推送，医生手机企业微信收到模板卡片
- [ ] 点击卡片跳转 H5 报告页（`h5.base_url` 需域名备案后配置）
- [ ] `GET /api/v1/admin/push/logs` 可查 SUCCESS/FAILED/DEFERRED 状态
- [ ] 免打扰时段（22:00-08:00）推送置为 DEFERRED，次日 06:30 定时任务自动补推

---

### 任务 3.4：H5 后端接口就绪（✅ 2026-08-03 完成）

| 属性 | 内容 |
|------|------|
| 状态 | ✅ 已完成 |

**验收标准：**
- [x] `GET /api/v1/report/detail`（含缺陷汇总、缺陷明细、isConfirmed）
- [x] `POST /api/v1/report/confirm`（qc_confirm 落库 + 越权校验）
- [x] `GET /api/v1/doctor/tasks`（我的待整改，分页）
- [x] `GET /api/v1/dept/stats`（科室统计：缺陷率/等级分布/确认率）
- [x] `GET/POST/PUT/DELETE /api/v1/admin/rules`、`GET/POST /api/v1/admin/doctors`
- [x] `GET /api/v1/admin/push/logs`、`GET /api/v1/admin/sync/logs`

---

## 三、本周交付物清单

| 交付物 | 类型 | 状态 |
|--------|------|------|
| expr 引擎修复（get/str/num/before/after/hoursSince + 中缀 contains/matches） | 代码 | ✅ |
| 质控引擎单元测试（11 类运算符） | 测试 | ✅ |
| app 装配层 + 定时任务接线（同步/质控/推送） | 代码 | ✅ |
| PushService 推送编排 + 免打扰 + 推送日志 | 代码 | ✅ |
| 确认整改 qc_confirm + 科室统计接口 | 代码 | ✅ |
| 真实数据质控跑批验证 | 验证 | ⏳ 待数据/规则 |
| 企微真机推送联调 | 验证 | ⏳ 待 IP 加白 |
| 首批业务规则清单 | 文档 | ⏳ 待 PM/质控科 |

---

## 四、本周风险

| 风险 | 可能性 | 影响 | 应对 |
|------|--------|------|------|
| 企微可信 IP 未加白 | 中 | 推送真机联调阻塞 | 已产出阻碍协调报告，PM 跟进；先用 CSV 兜底医生映射 |
| 首批规则清单未到位 | 中 | 引擎无法按业务规则验收 | 先用 10 条示例规则联调，清单到位后增量导入 |
| H5 域名备案未完成 | 中 | 卡片跳转地址不可用 | 备案期间用内网 IP + `h5.base_url` 占位联调 |
| 规则误判（真实数据） | 中 | 错误推送影响医生信任 | 灰度：先 1 个科室 20% 医生试推（M7 上线步骤） |

---

## 五、M3/M4 完成检查清单

- [ ] `go build ./...` + `go test ./...` 通过（✅ 当前保持）
- [ ] 真实数据质控跑批成功，`qc_result`/`qc_status` 正确
- [ ] 二次质控幂等（同病例不重复写缺陷）
- [ ] 企微推送真机收到模板卡片，跳转 H5 正常
- [ ] push_log 状态可追溯（SUCCESS/FAILED/DEFERRED）
- [ ] 免打扰时段 DEFERRED 后自动补推
- [ ] 规则 CRUD 实测通过
- [ ] 交接文档 M3/M4 状态更新 + 代码已提交到 Git
